/**
 * @what Test setup configuration for Vitest + React Testing Library
 * @why Extends matchers with jest-dom for better DOM assertions
 * @exports None - side-effect only configuration
 */
import '@testing-library/jest-dom'
